rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Fry Sushi — libera leitura/escrita do app (contas, pedidos, cupons, imagens)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
